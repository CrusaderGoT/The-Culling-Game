{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": ["alembic stamp head", "alembic upgrade head"],
    "startCommand": "fastapi run app/api/main.py"
  }
}
