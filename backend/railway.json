{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": [
      "python app/scripts/drop_tables.py && alembic upgrade head"
    ],
    "startCommand": "fastapi run app/api/main.py"
  }
}
